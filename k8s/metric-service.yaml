apiVersion: v1
kind: Service
metadata:
  name: pod-security-webhook-metrics
  namespace: webhook-system
spec:
  selector:
    app: pod-security-webhook
  ports:
    - name: metrics
      port: 9091
      targetPort: 9091